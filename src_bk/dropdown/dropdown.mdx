import { Story, ArgsTable, Canvas } from "@storybook/blocks";
import * as DropdownStories from "./Dropdown.stories";

# Dropdown 下拉菜单

弹出窗口是一个按需打开的小型覆盖层。它们让用户可以访问其他内容和操作，而不会造成页面混乱。

<Canvas>
  <Story of={DropdownStories.Default} />
</Canvas>

## 组件 API

<ArgsTable />
